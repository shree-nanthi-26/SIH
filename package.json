{
  "name": "skillvista",
  "version": "1.0.0",
  "description": "SkillVista — AI-powered competency assessment platform (SIH 2026)",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"FRONTEND,BACKEND,AI\" --prefix-colors \"cyan,green,magenta\" \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:ai\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev:ai": "npm --prefix ai-modules run dev",
    "build": "npm --prefix frontend run build",
    "install:all": "npm --prefix frontend install && npm --prefix backend install && npm --prefix ai-modules install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
